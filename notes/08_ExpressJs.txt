9. Express.js

1. What is Express.js
2. Nedd of Express
3. Installing Express.js
4. Adding middlware
5. sending Response
6. Express Deepdive
7. Handling Routs

9.1 What is Express.js
	1.Express.js is a minimal and flexible web application framework for node.js
	2.It provides a robust set of features for building single-page,multi page, and hybird web 	  app.
	3. Express.js Simplifies server-side coding by Providig a layer of fundmetal web application.

	middleware : thodo thodo code bits ma karido 

9.2 Need of Express.js
	1. Express js simplifies server creation
	2. Routing Management
	3. Middleware Support
	4. Api Development 
	5. Community and plugins 

9.3 Installing Express.js
	same package ke liye
	cmd -> npm install express
	:Temoproy use
	cmd -> npm install express --no-save
	
9.4 Adding middleware
	Request -> middleware next() -> middleware res.end() response

	Example: loging midd -> user Auth midd -> json parsing midd -> static file midd  

9.5 sending response
	code : 
// core module
const http = require('http');

// global module
const express = require('express');

// local module user create
const requestHanler = require('./user');

 const app = express();

app.use((req,res,next)=>{
    console.log("Came in first middleware",req.url , req.method)
    next();
 })

app.use((req,res,next)=>{
    console.log("Came in second middleware",req.url , req.method)
    res.send("<h1>hello this Express js")
 })

const server = http.createServer(app)

const PORT = 3001;

server.listen(PORT,()=>{
    console.log(`Server Runing on Address http://localhost:${PORT}`);
});


9.6 Express deepdrive

9.7 handling Routes
	1. order matters
	2. can not call next() after send()
	3. "/" matcges everything 
	4. calling res.send implicitly calls res.send()


	code : 
	app.use("/",(req,res,next)=>{
    		console.log("Came in first middleware",req.url , req.method)
    		//  res.send("<h1>hello welcome first middleware </h2>")
    		next();
	 })

 	app.use("/",(req,res,next)=>{
    		console.log("Came in another middleware",req.url , req.method)
     		res.send("<h1>hello another first middleware </h2>")
    		// next();
 	})

	app.use("/submit-details",(req,res,next)=>{
    		console.log("Came in second middleware",req.url , req.method)
    		res.send("<h1>hello this Express js</h2>")
 	})

	after change code 
	app.get("/",(req,res,next)=>{
    console.log("Came in first middleware",req.url , req.method)
    //  res.send("<h1>hello welcome first middleware </h2>")
    next();
 })

 app.post("/submit-details",(req,res,next)=>{
    console.log("Came in second middleware",req.url , req.method)
    res.send("<h1>hello this Express js</h2>")
 })

 app.use("/",(req,res,next)=>{
    console.log("Came in another middleware",req.url , req.method)
     res.send("<h1>hello another first middleware </h2>")
    // next();
 })



	

	